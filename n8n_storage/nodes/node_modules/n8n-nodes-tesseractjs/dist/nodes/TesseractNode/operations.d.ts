import { IExecuteFunctions, INodeExecutionData } from "n8n-workflow";
import type { Worker } from "tesseract.js";
type BoundingBox = {
    top: number;
    left: number;
    width: number;
    height: number;
};
export declare function performOCR(this: IExecuteFunctions, worker: Worker, item: INodeExecutionData, itemIndex: number, imageFieldName: string, bbox?: BoundingBox, timeout?: number): Promise<INodeExecutionData>;
export declare function extractBoxes(this: IExecuteFunctions, worker: Worker, item: INodeExecutionData, itemIndex: number, imageFieldName: string, granularity: "paragraphs" | "lines" | "words" | "symbols", bbox?: BoundingBox, timeout?: number): Promise<INodeExecutionData>;
export {};
